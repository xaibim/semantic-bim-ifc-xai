# End-to-End Public Example (Stored Replay Record)

> **Record type**: `STORED REPLAY RECORD`.
> This is a record already present in
> [`sample20/sample20_public_records.jsonl`](../../sample20/sample20_public_records.jsonl).
> It is **not** a new live inference and **not** an interactive demonstration.
> It is reproduced here exactly as stored so a reviewer can follow the full
> contract end to end.

## Three ways this example can appear

| Mode | Meaning | Source of truth |
| --- | --- | --- |
| Stored replay record | A record already committed to the public `sample20` JSONL. | `sample20/sample20_public_records.jsonl` |
| Interactive conceptual demonstration | A non-authoritative illustration produced by the Harness Space. | `spaces/huggingface_harness/` |
| Live model inference | Output generated by a model at request time. | Not published in this artifact. |

The example below is the **stored replay** mode.

## Scope of the example

- One public record (`sample_id = 4eeac340747306fd`).
- Discipline: `structural_elements`; major IFC class group: `structure_primary`.
- Semantic task: `semantic_enrichment` of a vertical structural element whose
  applicable IFC class is ambiguous from the request alone.

## Input summary (explicitly marked as input)

```json
{
  "discipline": "structural_elements",
  "major_ifc_class_group": "structure_primary",
  "semantic_type": "semantic_enrichment",
  "ambiguity_flags": ["ambiguous_ifc_class"],
  "missing_inputs": ["ifc_classes"],
  "recovery_type": "class_disambiguation"
}
```

## Model output

```json
{
  "intent_class": "unknown_edge",
  "semantic_type": "semantic_enrichment",
  "ifc_class": "IfcColumn",
  "value_mode": "PREVIEW",
  "normalized_dimensions_m": {"height": 0.8, "length": 1.6, "width": 0.4},
  "required_psets": ["Pset_QuantityTakeOff", "Pset_ColumnCommon", "Pset_SlabCommon"],
  "required_relationships": ["IfcRelConnectsElements", "IfcRelDefinesByProperties"],
  "missing_inputs": ["ifc_classes"],
  "ambiguity_flags": ["ambiguous_ifc_class"],
  "recovery_needed": false,
  "recovery_type": "class_disambiguation",
  "safe_next_action": "Request confirmation of the applicable IFC class before proceeding.",
  "reason_codes": [
    "semantic_type::semantic_enrichment",
    "intent::unknown_edge",
    "ambiguity::ambiguous_ifc_class",
    "recovery::class_disambiguation",
    "evidence::zoning_matrix"
  ],
  "evidence_trace": {
    "evidence_pattern": "zoning_matrix",
    "relation_observed": "IfcRelConnectsElements",
    "ambiguity_context": "ambiguous_ifc_class"
  }
}
```

## Key fields

| Field | Value |
| --- | --- |
| `ifc_class` | `IfcColumn` |
| `semantic_type` | `semantic_enrichment` |
| `intent_class` | `unknown_edge` |
| `value_mode` | `PREVIEW` |
| `required_psets` | `Pset_QuantityTakeOff`, `Pset_ColumnCommon`, `Pset_SlabCommon` |
| `required_relationships` | `IfcRelConnectsElements`, `IfcRelDefinesByProperties` |
| `missing_inputs` | `ifc_classes` |
| `ambiguity_flags` | `ambiguous_ifc_class` |
| `recovery_needed` | `false` |
| `recovery_type` | `class_disambiguation` |
| `safe_next_action` | Request confirmation of the applicable IFC class before proceeding. |
| `reason_codes` | `semantic_type::semantic_enrichment`, `intent::unknown_edge`, `ambiguity::ambiguous_ifc_class`, `recovery::class_disambiguation`, `evidence::zoning_matrix` |

## Evidence trace

```json
{
  "evidence_pattern": "zoning_matrix",
  "relation_observed": "IfcRelConnectsElements",
  "ambiguity_context": "ambiguous_ifc_class"
}
```

The `evidence_trace` links the output to a named input context pattern
(`zoning_matrix`) and the relationship used (`IfcRelConnectsElements`). It is a
provenance/supportedness signal, not a mathematical attribution.

## Canonical check

```json
{
  "ok": true,
  "ifc_class": "IfcColumn",
  "value_mode": "PREVIEW",
  "errors": [],
  "warnings": ["reason_codes_at_maximum"]
}
```

## Agreement

```json
{
  "ifc_class": true,
  "semantic_type": true,
  "intent_class": true,
  "value_mode": true,
  "dimensions": true,
  "missing_inputs": true,
  "required_psets_recall": 1.0,
  "required_relationships_recall": 1.0
}
```

## Reference scope

```json
"reference_scope": "synthetic_target_not_normative_certification"
```

The reference target is a **synthetic** enriched target. It is **not** a
normative certification and does not assert compliance with any specific IFC
schema version (IFC2x3 or IFC4) or building code.

## Limitations of this example

- It is a single stored synthetic record, not a representative corpus.
- The IFC class name (`IfcColumn`) is an entity label used in the contract; it
  does **not** by itself demonstrate full IFC2x3 or IFC4 coverage.
- `value_mode = PREVIEW` is a read-only preview; no mutation is authorized.
- `reference_scope` is explicitly synthetic and non-certifying.
- This record does **not** constitute a benchmark result, a generalization
  claim, or a multilingual claim.
- No live model inference or chain-of-thought is published here.
